# Perfil de desarrollo (local)

# Nombre del proyecto
spring.application.name=Udipsai

# Puerto del servidor
server.port=8080

# Configuraciï¿½n de JDBC (Base de datos)
spring.datasource.url=jdbc:postgresql://localhost:5432/udipsai
spring.datasource.username=postgres
spring.datasource.password=Pumamunoz14
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Hibernate (Crea y actualiza las tablas de la base de datos)
# Se usa auto update solo en este perfil
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Configuracion de JWT
jwt.secret=OnePieceElBestoShonenAbsolutePeakAndCinema3D2Y
jwt.expiration=86400000

# Logging
logging.level.root=INFO
logging.level.ec.ucacue=DEBUG
logging.level.org.springframework.security=WARN

# Swagger
spring.data.rest.detection-strategy=annotated